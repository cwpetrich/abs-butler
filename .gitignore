node_modules/
dist/
*.tsbuildinfo

# Secrets and local config
.env
.env.local
config.json
config.local.json

# Runtime output
*.log
.cache/
out/
reports/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Snap build artifacts. snapcraft creates these beside the source; only
# snap/ itself (snapcraft.yaml, hooks, wrappers, icon) is tracked.
*.snap
parts/
prime/
stage/
